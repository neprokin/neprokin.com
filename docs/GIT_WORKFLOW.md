# 🔧 Git Workflow для neprokin.com

## 🚨 Предотвращение проблем с поиском коммитов

### **Проблема, которую мы решили:**
- Коммит 4e9e6b7 был создан вчера, но не найден в локальной истории
- Неправильный поиск привел к путанице
- Отсутствие системного подхода к работе с Git

### **Решение:**

#### **1. Правильные команды поиска:**
```bash
# ✅ Поиск по хешу (частичному)
git log --oneline --all | grep "4e9e6b7"

# ✅ Поиск по сообщению
git log --oneline --all --grep="domain setup"

# ✅ Поиск по автору
git log --oneline --all --author="neprokin"

# ✅ Поиск по дате
git log --oneline --all --since="yesterday"
```

#### **2. Регулярная синхронизация:**
```bash
# Перед началом работы
git fetch origin
git status

# После работы
git push origin main
```

#### **3. Использование скриптов:**
```bash
# Проверка состояния проекта
./scripts/check-project-status.sh

# Синхронизация
git sync
```

### **Лучшие практики:**

#### **Ежедневный workflow:**
1. `git fetch origin` - получить обновления
2. `git status` - проверить состояние
3. Работа с кодом
4. `git add .` - добавить изменения
5. `git commit -m "описание"` - закоммитить
6. `git push origin main` - отправить изменения

#### **При поиске коммитов:**
1. Использовать `git log --oneline --all --graph`
2. Искать по ключевым словам в сообщениях
3. Проверять все ветки (`--all`)
4. Использовать полные хеши при необходимости

#### **При восстановлении:**
1. Создать резервную копию (`git stash`)
2. Переключиться на коммит (`git checkout <hash>`)
3. Создать новую ветку (`git switch -c <branch-name>`)
4. Проверить корректность восстановления

### **Автоматизация:**

#### **Pre-commit hook:**
- Автоматическая синхронизация перед коммитом
- Проверка статуса проекта
- Предотвращение проблем с синхронизацией

#### **Скрипты:**
- `check-project-status.sh` - полная проверка состояния
- Git алиасы для частых операций
- Автоматизация рутинных задач

### **Мониторинг:**

#### **Регулярные проверки:**
- Еженедельная проверка состояния проекта
- Мониторинг синхронизации с удаленным репозиторием
- Проверка целостности истории коммитов

#### **Алерты:**
- Уведомления о конфликтах
- Предупреждения о несинхронизированных изменениях
- Контроль целостности веток

---

**Результат:** Системный подход к работе с Git предотвращает повторение проблем с поиском коммитов и обеспечивает стабильную работу с версиями проекта.
